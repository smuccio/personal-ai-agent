{
  "name": "personal-ai-agent",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=packages/web",
    "agent": "npm run start --workspace=packages/cli --"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
